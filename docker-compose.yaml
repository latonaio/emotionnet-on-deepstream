version: "3"
services:
  emotionnet-tao-toolkit:
    container_name: emotionnet-tao-toolkit
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - ./:/app/src
    working_dir: /app/src
    tty: true
